#include "main.h"
/**
 * _abs - Computes the absolute number of a number
 * @n: Integer to make the test in.
 * Return: n if the number is positive. -n if the number is negative
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	return (-n);
}
