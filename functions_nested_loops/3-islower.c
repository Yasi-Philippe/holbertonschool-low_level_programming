#include "main.h"
/**
 * print_alphabet_x10 - prints 10 times the alphabet
 *
 * Return: 1 if c is lowercase, 0 if it's not.
 */
int _islower(int c)
{
	if (c > 96 && c < 123)
		return (1);
	return (0);
}
